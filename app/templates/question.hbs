<h1>{{model.question}}</h1>
<p><i>Asked by {{model.author}}</i></p>
<p>{{model.notes}}</p>
<p>{{model.timeStamp}}</p>

<hr>
{{new-answer saveAnswer="saveAnswer" question=model}}

<ul class="answer-box">
  {{#each model.answers as |answer|}}
    <hr>
    <p><i>{{answer.author}} says</i></p>
    <p>{{answer.answer}}</p>
    <p>{{timeStamp answer}}</p>
  {{/each}}
</ul>
